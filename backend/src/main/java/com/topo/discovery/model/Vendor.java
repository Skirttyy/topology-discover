package com.topo.discovery.model;

/**
 * Vendorii suportati de aplicatie.
 * Fiecare vendor are un VendorAdapter propriu cu comenzi specifice
 * (vezi pachetul com.topo.discovery.vendor).
 */
public enum Vendor {
    JUNIPER,
    ARISTA,
    UNKNOWN
}
